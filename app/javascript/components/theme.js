import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    darkGreen: '#1e352f',
    lightGreen: '#335145',
    gray: '#eeeeee',
    blue: '#5c9ead',
  },
});

export default theme;